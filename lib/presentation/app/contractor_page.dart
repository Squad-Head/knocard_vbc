import 'package:flutter/material.dart';

class BlankContractorPage extends StatelessWidget {
  const BlankContractorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blank Contractor Page'),
    );
  }
}

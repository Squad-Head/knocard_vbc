import 'package:flutter/material.dart';

class BlankGalleryPage extends StatelessWidget {
  const BlankGalleryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blank Gallery Page'),
    );
  }
}

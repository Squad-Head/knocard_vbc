import 'package:flutter/material.dart';

class BlankReviewsPage extends StatelessWidget {
  const BlankReviewsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blank Reviews Page'),
    );
  }
}

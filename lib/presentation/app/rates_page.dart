import 'package:flutter/material.dart';

class BlankRatesPage extends StatelessWidget {
  const BlankRatesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blank Rates Page'),
    );
  }
}
